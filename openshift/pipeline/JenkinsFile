#!/usr/bin/env groovy

pipeline {

  agent {
	  label 'maven-slave'
	}

  stages{
    stage('Init'){
      steps {
        script {
          sh 'echo "Hello from the pipeline" '
        }
      }
    }   
  }
}


// oc new-app https://github.com/rhappdev/nodejs-template.git --context-dir=openshift/pipeline --name <<name of the pipeline>>

